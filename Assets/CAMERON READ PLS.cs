using UnityEngine;
using System.Collections;

public class CAMERONREADPLS : MonoBehaviour {

	// Use this for initialization
	void Start () {
	/*
	 * 
	 * I attempted to generate the world as I went using Instantiate() whent the play went through the red block trigger, but I kept getting errors
	 * and couldn't get a new tabletop to generate when the player went through it
	 * 
	 * I attached the camera to the player, so that it follows its movement so there's no way for the camera to lose sync
	 * I commented out your code for it but it's still there if you wanna use that instead
	 * 
	 * movement script hasnt been changed
	 * 
	 * */
	}
	
	// Update is called once per frame
	void Update () {
		
	
	}
}
